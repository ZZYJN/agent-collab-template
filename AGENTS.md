# <项目名> — 项目协作规范（根规范）

本文件是项目的**根规范与索引**，面向所有协作者：人类工程师与所有 AI 代理。`docs/workflow.md` 等为受控子规范（修改同样须人批准）；**规则冲突时以本文件为准**。Codex 原生读取本文件；Claude Code 经 `CLAUDE.md` 的 `@AGENTS.md` 导入读取；接入其他 agent 一律给一行导入/指针文件，禁止复制规范内容。

## 项目概述

> ⚠️ 待补充：项目做什么、目标用户、数据类型与敏感度、对外暴露面、可用性要求。
> 本节是风险基线：完善之前代理不得对项目方向自行假设，首个 PRD 立项前必须先补齐本节。

## 核心工作原则（怎么思考）

1. **文件是唯一的共享记忆。** 代理之间、人与代理之间不共享会话上下文。任何需要别人（或未来的自己）知道的信息必须落盘（任务书、文档、ADR、DEVLOG）。只说在对话里等于没说。
2. **文档先行（normal-lite 及以上）。** 此类代码改动必须能追溯到 status 为 approved 的上游文档（normal-lite 追溯到既有已批 RFC）；实现中发现文档错了，先停手、按升版流程重批，再改代码。trivial 级豁免文档，但须在 commit 正文写明原因与验证方式（分级定义见 workflow §1）。
3. **不臆测。** 不确定就查证或问人；必须假设时写进任务书或 RFC 附录。
4. **最小改动。** 只改当前变更范围内的东西（有任务书按任务书，trivial 按其 commit 说明），不顺手重构、不碰禁区。
5. **如实汇报。** 没跑、没过、没验证就明说；结论带证据（报错摘录、复现步骤、测试输出）。**证据入库前先脱敏**：密钥、token、个人信息一律打码，敏感原文不入库。

## 角色与分工

流程绑定**角色**而非厂商（ADR-002），换扮演者不换流程：

- **编排者 / 评审者**（当前：Claude Code）：需求澄清、RFC、拆任务书、派工、依据文档做符合性评审、**执行流程内的全部 git 提交**。
- **实现者**（当前：Codex）：按任务书写代码和测试，回写实现报告。**不越界、不执行 commit、不改任何文档的批准状态。**
- **人类**：批准闸门——PRD（主研发 + PM）、RFC（主研发）、敏感动作签核（见下）。**文档的 approved 状态只能由人签出。**

**敏感动作签核（阻断式，不是抽查）**——以下动作必须先获人类明确批准，agent 不得先斩后奏：密钥/凭证的创建、轮换与使用方式变更；权限模型变更；支付相关改动；用户数据的批量删除或迁移；新增对外接口或暴露面；生产环境配置变更。

铁律：**写者不当查者**——实现产出必须经另一角色评审后才能提交（仅 trivial 级豁免，其约束见 workflow §1）。

## 并发与写入纪律

- **同一时刻，仓库只允许一个活动写者会话**（人或 agent）。编排者在 `codex exec` 运行期间不得并行修改仓库。
- 会话开始先 `git status` 并读 DEVLOG 末尾几条；来源不明的未提交改动不覆盖、不丢弃，问人。
- 需要真正并行时（触发器见 workflow §6）：per-task worktree + 集成队列——届时"单写者"变为每个 worktree 一个写者，任务号由编排者统一预分配，集成仍串行；未启用前不并行。

## Git 纪律

- 流程内提交由编排者（或人）执行。commit message 首行以 `[claude]` / `[codex]` / `[human]` 开头，**前缀标注改动的主要作者**（实现类提交为 `[codex]`）；normal-lite 及以上须引用任务号并带 trailer：`Implemented-by:` / `Reviewed-by:`。
- 评审通过后立即做**实现提交**；随后单独做**收尾提交**（`[claude] T-NNN 收尾`：回填 commit SHA、status: done、CHANGELOG——首次 release 时才创建——及必要的 DEVLOG）。固定两段式：commit SHA 产生于提交之后，写不进它自身。
- 不 push（由人执行或经人授权）、不改写历史（唯一例外：密钥/PII 已进历史，经人批准的清理专项——先轮换吊销，再清历史）；禁止 `--no-verify`，确需绕过是人的决定并留痕。

## 流程入口（触发式索引，按需阅读）

- **接到新需求 / 变更 / 开工** → 必读 `docs/workflow.md`
- 起草 PRD / RFC / 任务书 → 用 `docs/templates/` 模板
- **长期、跨模块、难逆转**的技术取舍 → 写 ADR（`docs/decisions/`）；一次性迁移、可逆的依赖升级写进 RFC/任务书即可
- DEVLOG（`docs/DEVLOG.md`，只追加）只记录：**跨任务决定、重要交接、未归属的发现、流程异常**。普通任务进展写任务书，不重复记账。

## 工程规范

> ⚠️ 技术栈未定。技术栈 ADR 批准时必须同步给出：唯一的 `check` 命令（lint + 类型 + 测试 + 构建）、测试分层要求、格式化工具。此前不产生业务代码。

底线：密钥一律不入库；新增运行时依赖须锁定版本并经人确认；提交前跑通与改动相关的验证，无法自动化的检查须记录理由与责任人。

## 目录结构

```
AGENTS.md            根规范与索引（常驻上下文，≤150 行；冲突时以本文件为准）
CLAUDE.md            @AGENTS.md 导入 + Claude 专属补充
README.md            面向人类的门面：简介与导航；只镜像与链接
docs/
  workflow.md        受控子规范：分级、状态机、派工与评审协议、触发器清单
  prd/  design/      PRD 与 RFC（approved 才可作为开工依据）
  tasks/             任务书（T-NNN-*.md：派工契约 + 实现报告 + 评审记录 + 追溯字段）
  decisions/         ADR
  templates/         模板
  reviews/           独立审计 / 评审报告归档
  DEVLOG.md          协作日志（只追加；记录范围见「流程入口」）
scripts/hooks/       版本化 git 钩子（经 core.hooksPath 启用）
src/                 （技术栈确定后建立）
```

## 常用命令

> ⚠️ 待补充：安装 / 构建 / 测试 / 运行命令（技术栈 ADR 落定时填写）。
- 启用 git 钩子（新克隆后执行一次）：`git config core.hooksPath scripts/hooks`

## 本文件的维护

- 事实性内容（目录、命令）过时即改；规范性内容（原则、角色、协议）修改须经用户批准，并在 DEVLOG 留痕。
- 往本文件加内容先问"是否每个会话都需要"；不是就放 `docs/` 挂触发条件。个人偏好写各自全局配置，不入本文件。
